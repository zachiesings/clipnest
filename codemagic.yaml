# Build ClipNest (macOS) untuk Mac App Store di Codemagic.
# Penandatanganan memakai App Store Connect API key (env vars: APP_STORE_CONNECT_*,
# CERTIFICATE_PRIVATE_KEY) yang dikirim saat build dipicu lewat Codemagic API.

workflows:
  macos-appstore:
    name: ClipNest macOS — App Store
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      xcode: latest
      vars:
        BUNDLE_ID: "app.clipnest.ClipNest"
        XCODE_PROJECT: "ClipNest.xcodeproj"
        XCODE_SCHEME: "ClipNest"

    scripts:
      - name: Generate Xcode project (XcodeGen)
        script: |
          which xcodegen || brew install xcodegen
          xcodegen generate

      - name: Initialize keychain
        script: keychain initialize

      - name: Fetch Mac App Store signing files (create if needed)
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --platform MAC_OS \
            --type MAC_APP_STORE \
            --certificate-key "@env:CERTIFICATE_PRIVATE_KEY" \
            --create

      - name: Ensure Mac Installer Distribution certificate
        script: |
          app-store-connect certificates create \
            --type MAC_INSTALLER_DISTRIBUTION \
            --certificate-key "@env:CERTIFICATE_PRIVATE_KEY" \
            --save || echo "Installer certificate already available for this key."

      - name: Add certificates to keychain
        script: keychain add-certificates

      - name: Set up provisioning profiles
        script: xcode-project use-profiles

      - name: Archive (Release)
        script: |
          xcodebuild -project "$XCODE_PROJECT" -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -archivePath "$CM_BUILD_DIR/ClipNest.xcarchive" \
            CURRENT_PROJECT_VERSION=$BUILD_NUMBER \
            archive

      - name: Import installer certificate (.p12)
        script: |
          set -e
          echo "$INSTALLER_P12_BASE64" | base64 --decode > /tmp/installer.p12
          MYKC="$HOME/clipnest-installer.keychain-db"
          MYPW="clipnest"
          security create-keychain -p "$MYPW" "$MYKC"
          security set-keychain-settings -lut 21600 "$MYKC"
          security unlock-keychain -p "$MYPW" "$MYKC"
          security import /tmp/installer.p12 -k "$MYKC" -P "$INSTALLER_P12_PASSWORD" -A -T /usr/bin/productbuild -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$MYPW" "$MYKC"
          # Add our keychain to the search list (keep the existing Codemagic keychain too).
          EXISTING=$(security list-keychains -d user | sed 's/[" ]//g' | tr '\n' ' ')
          security list-keychains -d user -s "$MYKC" $EXISTING
          echo "=== identities in our keychain ==="
          security find-identity -v -p basic "$MYKC" || true

      - name: Build signed installer .pkg (productbuild)
        script: |
          set -e
          mkdir -p "$CM_BUILD_DIR/export"
          APP_PATH="$CM_BUILD_DIR/ClipNest.xcarchive/Products/Applications/ClipNest.app"
          echo "App path: $APP_PATH"
          echo "=== identities (basic) ==="
          security find-identity -v -p basic || true
          # Installer identities aren't listed under the codesigning policy; use -p basic and match by hash.
          INSTALLER_HASH=$(security find-identity -v -p basic 2>/dev/null | grep -i "Installer" | head -1 | awk '{print $2}')
          echo "Installer hash: $INSTALLER_HASH"
          if [ -z "$INSTALLER_HASH" ]; then
            echo "No installer identity in keychain"; exit 1
          fi
          productbuild --component "$APP_PATH" /Applications \
            --sign "$INSTALLER_HASH" \
            "$CM_BUILD_DIR/export/ClipNest.pkg"
          echo "=== built pkg ==="
          ls -la "$CM_BUILD_DIR/export"

      - name: Upload to App Store Connect
        script: |
          app-store-connect publish \
            --path "$CM_BUILD_DIR/export/*.pkg"

    artifacts:
      - $CM_BUILD_DIR/export/*.pkg
      - $CM_BUILD_DIR/*.xcarchive

    publishing:
      email:
        recipients:
          - asd95179517@gmail.com
        notify:
          success: true
          failure: true
